# 多 Skill 协同

当前主路径 **一个 Skill 闭环** 到 PPTX。

```text
pharma-courseware-replication
  → workspace/<template-id>/（结构 + 视觉 + 素材 + PPTX + change-list）
```

未来若出现「可单独安装调用」的需求，再拆：话术风险清单、纯素材缺件表等。  
**不**为协同而引入其他仓库依赖。

`slots.json` schema：**0.2**  
